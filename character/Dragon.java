package character;

public class Dragon extends Monster{
    public Dragon(String name, String image, Object attackAsset, Object defenseAsset, int lifePoints, int maxLifePoints, int attack, int maxAttack) {
        super(name, image, attackAsset, defenseAsset, lifePoints, maxLifePoints, attack, maxAttack);
    }
}
